### LOCAL TEST ENV - BACKEND
# NOTE: This file contains example credentials for local testing only.
# DO NOT commit a real secret in a public repo. You asked for a quick way
# to try things so the provided password is used below.

# Use a Supabase Postgres connection string (username:password@host:port/db)
DATABASE_URL=postgresql://postgres:192354768Gsensei@db.wpqxtvvszznoicfjblhk.supabase.co:5432/postgres

# The backend can validate Supabase JWTs if you set this secret.
# For local quick testing we'll set it here to the same password (not recommended in production).
SUPABASE_JWT_SECRET=192354768Gsensei

# Keep a local app-level JWT secret for legacy tokens (if still issuing from backend)
JWT_SECRET_KEY=local-legacy-jwt-secret

# CORS (comma separated origins)
CORS_ORIGINS=http://localhost:3000

# Admin (first-run) - optional
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com
