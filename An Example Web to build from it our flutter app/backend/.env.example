# Backend Environment Variables

# Security Keys (REQUIRED - Generate secure random strings)
SECRET_KEY=your-secret-key-here-change-this-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-this-in-production

# Optional: If using Supabase Auth, set the Supabase JWT secret here so the
# backend can verify Supabase-issued tokens.
SUPABASE_JWT_SECRET=

# Database Configuration
DATABASE_URL=sqlite:///./app.db

# Admin User Creation (Optional - for first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com
ADMIN_FULL_NAME=System Administrator

# Application Settings
DEBUG=False
ENVIRONMENT=production

# CORS Settings (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80

# JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=30
